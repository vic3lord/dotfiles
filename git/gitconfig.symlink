[user]
	name = Or Elimelech
	email = 0r3limelech@gmail.com
[alias]
	ci = commit
	co = checkout
	br = branch
	st = status -s
	edit = "!vim `git ls-files -m` -p"
[push]
	default = simple
[color]
	ui = true
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
